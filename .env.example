# Example environment variables
# Copy to .env for local development (git-ignored)

# Database
DATABASE_URL=postgres://postgres:postgres@db:5432/ho_mon_radeau_dev

# Phoenix
SECRET_KEY_BASE=changeme_generate_with_mix_phx_gen_secret
PHX_HOST=localhost
PHX_PORT=4000
MIX_ENV=dev

# Email (development with mailcatcher)
SMTP_HOST=mailcatcher
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@homonradeau.local

# Production (Fly.io)
# Set these as Fly.io secrets:
# fly secrets set SECRET_KEY_BASE=xxx
# fly secrets set DATABASE_URL=xxx
